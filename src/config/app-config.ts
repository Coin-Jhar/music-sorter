// src/config/app-config.ts
import { PATHS, SORT_PATTERNS } from './constants';

export const APP_CONFIG = {
  PATHS,
  DEFAULT_SORT_PATTERN: 'artist',
  DEFAULT_RENAME_PATTERN: '{artist} - {title}',
  DEFAULT_COPY_MODE: true,
};
